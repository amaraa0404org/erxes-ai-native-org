---
phase: 01.1-shared-module-plugin-scaffolds
plan: 03
type: execute
wave: 2
depends_on:
  - 01.1-01
files_modified:
  - frontend/plugins/ai_ui/module-federation.config.ts
  - frontend/plugins/ai_ui/rspack.config.ts
  - frontend/plugins/ai_ui/rspack.config.prod.ts
  - frontend/plugins/ai_ui/project.json
  - frontend/plugins/ai_ui/tsconfig.json
  - frontend/plugins/ai_ui/tsconfig.app.json
  - frontend/plugins/ai_ui/tsconfig.spec.json
  - frontend/plugins/ai_ui/jest.config.ts
  - frontend/plugins/ai_ui/eslint.config.js
  - frontend/plugins/ai_ui/src/main.ts
  - frontend/plugins/ai_ui/src/bootstrap.tsx
  - frontend/plugins/ai_ui/src/index.html
  - frontend/plugins/ai_ui/src/config.tsx
  - frontend/plugins/ai_ui/src/modules/Main.tsx
  - frontend/plugins/ai_ui/src/modules/AISettingsNavigation.tsx
  - frontend/plugins/ai_ui/src/modules/settings/AiSettingsIndexPage.tsx
  - frontend/plugins/ai_ui/src/Copilot.tsx
  - frontend/plugins/ai_ui/src/widgets/index.ts
  - frontend/plugins/ai_ui/src/hooks/index.ts
  - frontend/plugins/ai_ui/src/atoms/index.ts
  - frontend/libs/erxes-ui/src/ai/useAIAvailable.ts
  - frontend/libs/erxes-ui/src/ai/AiRemoteBoundary.tsx
  - frontend/libs/erxes-ui/src/ai/index.ts
  - frontend/libs/erxes-ui/src/index.ts
autonomous: true
requirements:
  - HOOK-02
  - UI-10
  - XCUT-04

must_haves:
  truths:
    - "`pnpm nx build ai_ui` exits 0 from a clean clone with Plan 01 already merged."
    - "`frontend/plugins/ai_ui/module-federation.config.ts` exposes EXACTLY these 6 entries on Day 1: `./config`, `./aiSettings`, `./Copilot`, `./atoms`, `./hooks`, `./widgets` (per CONTEXT D-06)."
    - "The `shared` block declares `react`, `react-dom`, `react-router`, `react-router-dom`, `erxes-ui`, `@apollo/client`, `jotai`, `ui-modules`, `react-i18next` as singletons with `requiredVersion` (NOT `strictVersion: true`) — verbatim mirror of `frontend/plugins/sales_ui/module-federation.config.ts:3-13` (per CONTEXT D-08, REQ HOOK-02)."
    - "`useAIAvailable()` hook returns `false` when `import('ai_ui/config')` rejects, cached for the lifetime of the host page, and exposes a manual `refetch()` (per CONTEXT D-15 Claude's-Discretion bullet 3 — leaning yes for Settings page's Test-Connection flow)."
    - "Consumer code can wrap any `ai_ui` remote import with `<AiRemoteBoundary fallback={...}>` and a MF hard-fail on missing remote renders the fallback instead of crashing the host (REQ UI-10)."
    - "Phase 1.1 placeholders are inert: `./Copilot` returns `<></>` with a `// stub: filled in Phase 2.3` comment; `./atoms` exports `{}` typed; `./hooks` exports an object whose every accessor throws `Error('useAIChat: implementation lands in Phase 3.2')`; `./widgets` exports an object whose every accessor returns `null` (per CONTEXT D-07)."
  artifacts:
    - path: frontend/plugins/ai_ui/module-federation.config.ts
      provides: "MF expose map (6 entries) + singleton shared config"
      contains: "singleton: true"
    - path: frontend/plugins/ai_ui/src/config.tsx
      provides: "CONFIG: IUIConfig with settingsNavigation pointing at AI Settings"
      contains: "IUIConfig"
    - path: frontend/plugins/ai_ui/src/Copilot.tsx
      provides: "Phase 1.1 stub Copilot (returns <></>) — Phase 2.3 replaces"
      contains: "Phase 2.3"
    - path: frontend/libs/erxes-ui/src/ai/useAIAvailable.ts
      provides: "MF-load probe hook returning { available, loading, refetch }"
      contains: "useAIAvailable"
    - path: frontend/libs/erxes-ui/src/ai/AiRemoteBoundary.tsx
      provides: "ErrorBoundary specialized for ai_ui remote imports"
      contains: "AiRemoteBoundary"
  key_links:
    - from: frontend/plugins/ai_ui/module-federation.config.ts
      to: frontend/plugins/sales_ui/module-federation.config.ts
      via: "coreLibraries Set copied verbatim from sales_ui:3-13"
      pattern: "'react'.*'react-dom'.*'react-router'.*'react-router-dom'.*'erxes-ui'.*'@apollo/client'.*'jotai'.*'ui-modules'.*'react-i18next'"
    - from: frontend/plugins/ai_ui/src/config.tsx
      to: frontend/libs/erxes-ui/src/types/UIConfig.ts
      via: "import type { IUIConfig } from 'erxes-ui'"
      pattern: "IUIConfig"
    - from: frontend/libs/erxes-ui/src/ai/useAIAvailable.ts
      to: frontend/plugins/ai_ui/src/config.tsx
      via: "dynamic import('ai_ui/config') probe in the hook"
      pattern: "ai_ui/config"
---

<objective>
Land the `frontend/plugins/ai_ui/` plugin skeleton — a Module Federation remote that builds, exposes its FINAL 6-entry MF surface (per CONTEXT D-06: `./config`, `./aiSettings`, `./Copilot`, `./atoms`, `./hooks`, `./widgets`) with all placeholders (per D-07) inert but typed, declares its `shared` block as a verbatim mirror of `sales_ui:3-13` (per D-08, REQ HOOK-02), and provides the `useAIAvailable()` MF-load probe + `<AiRemoteBoundary>` ErrorBoundary pattern (REQ UI-10) in `erxes-ui` so other plugin UIs can gate AI features safely even when the `ai_ui` remote fails to load.

Purpose: Phases 1.4, 2.3, and 3.2 fill in the placeholder modules without ever needing to touch the MF config. Phase 1.4 owns ProvidersPage/ModelsPage/BudgetsPage/AuditPage. Phase 2.3 replaces the Copilot stub with the real floating sidecar. Phase 3.2 replaces the hooks/widgets placeholders with real implementations. By shipping the MF surface DAY 1, every downstream phase is a content fill-in, not an architectural change.

Output: A fully buildable `ai_ui` plugin (`pnpm nx build ai_ui` exits 0) with 6 MF exposes, a complete file inventory per D-12, and the `useAIAvailable` + `AiRemoteBoundary` pattern shipped in `erxes-ui/ai` so any other plugin can `import { useAIAvailable, AiRemoteBoundary } from 'erxes-ui'`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.1-shared-module-plugin-scaffolds/01.1-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@CLAUDE.md

@frontend/plugins/sales_ui/module-federation.config.ts
@frontend/plugins/sales_ui/rspack.config.ts
@frontend/plugins/sales_ui/rspack.config.prod.ts
@frontend/plugins/sales_ui/project.json
@frontend/plugins/sales_ui/tsconfig.json
@frontend/plugins/sales_ui/tsconfig.app.json
@frontend/plugins/sales_ui/tsconfig.spec.json
@frontend/plugins/sales_ui/jest.config.ts
@frontend/plugins/sales_ui/eslint.config.js
@frontend/plugins/sales_ui/src/main.ts
@frontend/plugins/sales_ui/src/bootstrap.tsx
@frontend/plugins/sales_ui/src/index.html
@frontend/plugins/sales_ui/src/config.tsx
@frontend/libs/erxes-ui/src/types/UIConfig.ts
@frontend/libs/erxes-ui/src/index.ts
@package.json
@.planning/phases/01.1-shared-module-plugin-scaffolds/01.1-01-PLAN.md

<interfaces>
<!-- Existing IUIConfig type (frontend/libs/erxes-ui/src/types/UIConfig.ts) -->
export type IUIConfig = {
  name: string;
  path: string;
  icon?: React.ElementType;
  hasFloatingWidget?: boolean;
  settingsNavigation?: () => React.ReactNode;
  navigationGroup?: { name: string; icon: React.ElementType; content: () => React.ReactNode; subGroup?: () => React.ReactNode; };
  widgets?: { relationWidgets?: { name: string; icon?: React.ElementType }[]; customerDetailWidgets?: { name: string }[] };
  modules?: { name: string; icon?: React.ElementType; path: string; hasAutomation?: boolean; hasRelationWidget?: boolean; hasFloatingWidget?: boolean; hasSegmentConfigWidget?: boolean }[];
};

<!-- Existing sales_ui module-federation.config.ts (the verbatim mirror target per D-08) -->
<!-- lines 3-13: -->
const coreLibraries = new Set([
  'react', 'react-dom', 'react-router', 'react-router-dom',
  'erxes-ui', '@apollo/client', 'jotai', 'ui-modules', 'react-i18next',
]);
<!-- ai_ui MUST use the same Set verbatim. Per D-08: NO strictVersion: true. -->

<!-- Existing root package.json versions (for requiredVersion strings if needed) -->
"react": "18.3.1"
"react-dom": "18.3.1"
"react-router": "^7.1.5"
"react-router-dom": "^7.1.1"
"@apollo/client": "^3.11.8"
"jotai": "^2.12.0"
"react-i18next": "^15.1.1"
<!-- erxes-ui and ui-modules are workspace libs (no version pinning required) -->

<!-- The @nx/rspack/module-federation `shared: (libraryName, defaultConfig) => ...` callback -->
<!-- AUTOMATICALLY injects singleton: true + requiredVersion derived from root package.json when defaultConfig is returned. -->
<!-- Returning `false` excludes from sharing. -->
<!-- This is the same pattern sales_ui uses — no manual requiredVersion strings needed. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manually clone sales_ui into ai_ui as a skeleton (rspack, MF config, tsconfig, project.json, basic src files) — implements D-06/D-08/D-14</name>
  <files>
    frontend/plugins/ai_ui/module-federation.config.ts,
    frontend/plugins/ai_ui/rspack.config.ts,
    frontend/plugins/ai_ui/rspack.config.prod.ts,
    frontend/plugins/ai_ui/project.json,
    frontend/plugins/ai_ui/tsconfig.json,
    frontend/plugins/ai_ui/tsconfig.app.json,
    frontend/plugins/ai_ui/tsconfig.spec.json,
    frontend/plugins/ai_ui/jest.config.ts,
    frontend/plugins/ai_ui/eslint.config.js,
    frontend/plugins/ai_ui/src/main.ts,
    frontend/plugins/ai_ui/src/bootstrap.tsx,
    frontend/plugins/ai_ui/src/index.html
  </files>
  <read_first>
    frontend/plugins/sales_ui/module-federation.config.ts (lines 1-43 — coreLibraries set + exposes map + shared callback; MIRROR exactly with renames);
    frontend/plugins/sales_ui/rspack.config.ts + rspack.config.prod.ts (copy verbatim);
    frontend/plugins/sales_ui/project.json (Nx targets — sales port 3005 → ai port 3020 per CONTEXT env vars);
    frontend/plugins/sales_ui/tsconfig.json + tsconfig.app.json + tsconfig.spec.json + jest.config.ts + eslint.config.js (copy verbatim with rename);
    frontend/plugins/sales_ui/src/main.ts (`import('./bootstrap');` one-liner);
    frontend/plugins/sales_ui/src/bootstrap.tsx (StrictMode + ReactDOM.createRoot — copy verbatim);
    frontend/plugins/sales_ui/src/index.html (HTML shell — copy verbatim);
    .planning/phases/01.1-shared-module-plugin-scaffolds/01.1-CONTEXT.md §decisions D-06, D-08, D-14;
    .planning/research/PITFALLS.md §P11 (singleton + requiredVersion mandatory; NO strictVersion: true)
  </read_first>
  <action>
    Copy `frontend/plugins/sales_ui/` → `frontend/plugins/ai_ui/` then apply these EXACT renames/strips:
    1. `module-federation.config.ts`: Replace `name: 'sales_ui'` → `name: 'ai_ui'`. Replace the `exposes:` block with EXACTLY (per CONTEXT D-06): `{ './config': './src/config.tsx', './aiSettings': './src/modules/settings/AiSettingsIndexPage.tsx', './Copilot': './src/Copilot.tsx', './atoms': './src/atoms/index.ts', './hooks': './src/hooks/index.ts', './widgets': './src/widgets/index.ts' }`. The `coreLibraries` Set MUST be copied VERBATIM from sales_ui lines 3-13 — all 9 entries in the same order. The `shared` callback MUST be copied VERBATIM (returns `defaultConfig` for members, `false` otherwise). DO NOT add `strictVersion: true` (per D-08, PITFALLS §P11). DO NOT manually specify `requiredVersion` strings — `defaultConfig` injection from `@nx/rspack/module-federation` handles versions automatically. Add a top-of-file comment block: `// Phase 1.1 MF surface is FINAL — Phases 1.4/2.3/3.2 fill the placeholder modules, NOT this config.`
    2. `rspack.config.ts` + `rspack.config.prod.ts`: copy verbatim (only re-imports `./module-federation.config` so no edits needed beyond filename context).
    3. `project.json`: name `"sales_ui"` → `"ai_ui"`; every path `frontend/plugins/sales_ui` → `frontend/plugins/ai_ui`; `tsconfig.app.json` path likewise; serve port `3005` → `3020` (matches `.env.sample` `AI_UI_PORT=3020`). Keep all 3 Nx targets (build, serve, serve-static).
    4. `tsconfig.json` + `tsconfig.app.json` + `tsconfig.spec.json`: copy verbatim, then in `tsconfig.json` paths change every `frontend/plugins/sales_ui/` → `frontend/plugins/ai_ui/`. The `~/*`, `@/*` aliases keep the same shape (just re-pointed to ai_ui's src and src/modules).
    5. `jest.config.ts`: copy verbatim, change `displayName: 'sales_ui'` → `displayName: 'ai_ui'` and `coverageDirectory: '../../../coverage/frontend/plugins/sales_ui'` → `'../../../coverage/frontend/plugins/ai_ui'`.
    6. `eslint.config.js`: copy verbatim (rename references if any).
    7. `src/main.ts`: copy verbatim — single line `import('./bootstrap');`.
    8. `src/bootstrap.tsx`: copy verbatim — StrictMode + ReactDOM.createRoot rendering `<div>App</div>` (Phase 1.1 host page is intentionally trivial — `ai_ui` runs as a remote, the host UI in core-ui handles the real rendering).
    9. `src/index.html`: copy verbatim.
    Implements decisions D-06 (final 6 exposes), D-08 (verbatim shared block; no strictVersion), D-14 (manual copy, no `pnpm create-plugin`).
  </action>
  <verify>
    <automated>ls frontend/plugins/ai_ui/{module-federation.config.ts,rspack.config.ts,rspack.config.prod.ts,project.json,tsconfig.json,tsconfig.app.json,tsconfig.spec.json,jest.config.ts,eslint.config.js} frontend/plugins/ai_ui/src/{main.ts,bootstrap.tsx,index.html} 2>&1; grep -c "name: 'ai_ui'" frontend/plugins/ai_ui/module-federation.config.ts; grep -E "'./config'|'./aiSettings'|'./Copilot'|'./atoms'|'./hooks'|'./widgets'" frontend/plugins/ai_ui/module-federation.config.ts | wc -l | grep -q '^[[:space:]]*6$' || (echo "Expected 6 expose entries" && grep -E "'./" frontend/plugins/ai_ui/module-federation.config.ts && false); grep -c "strictVersion: true" frontend/plugins/ai_ui/module-federation.config.ts | grep -q '^0$' || (echo "FORBIDDEN strictVersion: true present" && false); for lib in react react-dom react-router react-router-dom erxes-ui '@apollo/client' jotai ui-modules react-i18next; do grep -q "'$lib'" frontend/plugins/ai_ui/module-federation.config.ts || (echo "Missing shared lib: $lib" && false); done; grep -c '"name": "ai_ui"' frontend/plugins/ai_ui/project.json; grep -c '3020' frontend/plugins/ai_ui/project.json</automated>
  </verify>
  <done>All 12 config/source files exist; MF config has `name: 'ai_ui'`, exactly 6 exposes, no `strictVersion: true`, all 9 sales_ui coreLibraries entries present; project.json has `name: 'ai_ui'` and serve port `3020`; tsconfig paths point to `frontend/plugins/ai_ui/`.</done>
</task>

<task type="auto">
  <name>Task 2: Land the 6 MF expose modules as Phase 1.1 stubs (config.tsx wires AI Settings; Copilot/atoms/hooks/widgets are inert placeholders per D-07)</name>
  <files>
    frontend/plugins/ai_ui/src/config.tsx,
    frontend/plugins/ai_ui/src/modules/Main.tsx,
    frontend/plugins/ai_ui/src/modules/AISettingsNavigation.tsx,
    frontend/plugins/ai_ui/src/modules/settings/AiSettingsIndexPage.tsx,
    frontend/plugins/ai_ui/src/Copilot.tsx,
    frontend/plugins/ai_ui/src/widgets/index.ts,
    frontend/plugins/ai_ui/src/hooks/index.ts,
    frontend/plugins/ai_ui/src/atoms/index.ts
  </files>
  <read_first>
    frontend/plugins/sales_ui/src/config.tsx (canonical IUIConfig shape with lazy + Suspense pattern — settingsNavigation: () => <Suspense><SalesSettingsNavigation /></Suspense>);
    frontend/libs/erxes-ui/src/types/UIConfig.ts (IUIConfig type — note: `hasSettings` is NOT a field on `modules[]`; settings goes via `settingsNavigation` callback at the top level per RESEARCH §15.3 closed decision);
    .planning/phases/01.1-shared-module-plugin-scaffolds/01.1-CONTEXT.md §decisions D-06, D-07, D-12 (frontend file inventory)
  </read_first>
  <behavior>
    - `src/config.tsx`: Export `CONFIG: IUIConfig = { name: 'ai', path: 'ai', icon: IconRobot, settingsNavigation: () => <Suspense fallback={<div />}><AISettingsNavigation /></Suspense>, navigationGroup: { name: 'ai', icon: IconRobot, content: () => <Suspense fallback={<div />}><Main /></Suspense> }, modules: [{ name: 'ai', icon: IconRobot, path: 'ai' }] }`. `IconRobot` from `@tabler/icons-react`. `Main` and `AISettingsNavigation` are lazy-imported via `lazy(() => import('./modules/Main').then(m => ({ default: m.Main })))` and `lazy(() => import('./modules/AISettingsNavigation').then(m => ({ default: m.AISettingsNavigation })))`.
    - `src/modules/Main.tsx`: `export const Main = () => <div data-ai-main-stub>AI plugin loaded (Phase 1.1 — settings UI lands in Phase 1.4)</div>;` — informational div so navigating to `/ai` doesn't 404 during development.
    - `src/modules/AISettingsNavigation.tsx`: `export const AISettingsNavigation = () => <nav data-ai-settings-nav-stub>{/* Phase 1.4 fills providers/models/budgets/audit links */}</nav>;` — empty nav so injection point exists.
    - `src/modules/settings/AiSettingsIndexPage.tsx` (the `./aiSettings` MF target): `export default function AiSettingsIndexPage() { return <div data-ai-settings-index-stub>AI Settings (Phase 1.4 will render Providers/Models/Budgets/Audit tabs here)</div>; }`. The settings index page MUST `export default` because the host's MF consumer pattern (per sales_ui's `./dealsSettings` exposing `./src/pages/SalesSettingsIndexPage.tsx`) imports the default export.
    - `src/Copilot.tsx` (the `./Copilot` MF target): per CONTEXT D-07 bullet 1: `// stub: filled in Phase 2.3\nexport default function Copilot() { return <></>; }` — default-export fragment with the exact comment string `// stub: filled in Phase 2.3` so a future grep `Phase 2.3` flags this file as the replacement target.
    - `src/atoms/index.ts` (the `./atoms` MF target): per CONTEXT D-07 bullet 2: `// stub: filled in Phase 2.3\nexport const aiAtoms = {} as const;\nexport default aiAtoms;` — empty typed object plus default export (so a consumer can `import atoms from 'ai_ui/atoms'` or `import { aiAtoms } from 'ai_ui/atoms'`).
    - `src/hooks/index.ts` (the `./hooks` MF target): per CONTEXT D-07 bullet 3: every hook accessor throws. Export EXACTLY these named exports — `useAIChat`, `useAIDraft`, `useAISuggest`, `useAIClassify`, `useRagSearch` — each a no-arg function `() => { throw new Error('useAIChat: implementation lands in Phase 3.2'); }` (substitute the hook name). Also `export default { useAIChat, useAIDraft, useAISuggest, useAIClassify, useRagSearch };` so default imports work too. This enforces visible failure if anyone tries to call these hooks before Phase 3.2 — silent `undefined` would be worse per D-07's typed-stub policy.
    - `src/widgets/index.ts` (the `./widgets` MF target): per CONTEXT D-07 bullet 4: every widget returns null. Export named: `AIChatPanel`, `AIDraftEditor`, `AISuggestButton`, `AIRagSearchBox` — each a `() => null` typed `React.FC`. Also default-export `{ AIChatPanel, AIDraftEditor, AISuggestButton, AIRagSearchBox }`. (Phase 3.2 replaces the bodies.)
    Implements decisions D-06 (final MF expose targets exist), D-07 (placeholder bodies for the 4 not-yet-built exposes), D-12 (frontend file inventory bullets 6-9), HOOK-02 indirectly (the MF surface is in place; the singleton config in Task 1 is the actual HOOK-02 criterion).
  </behavior>
  <action>
    Create the 8 source files per the `<behavior>` block. ALL identifiers must be exact: `IconRobot` (from `@tabler/icons-react` — exists per existing imports across the codebase), `<div data-ai-main-stub>` attribute name (so future E2E tests can locate the stub), error message strings (`useAIChat: implementation lands in Phase 3.2` etc.), default + named export pairings. Implements D-06, D-07, D-12 frontend bullets.
  </action>
  <verify>
    <automated>ls frontend/plugins/ai_ui/src/{config.tsx,Copilot.tsx} frontend/plugins/ai_ui/src/modules/{Main.tsx,AISettingsNavigation.tsx,settings/AiSettingsIndexPage.tsx} frontend/plugins/ai_ui/src/{atoms/index.ts,hooks/index.ts,widgets/index.ts} 2>&1; grep -c "IUIConfig" frontend/plugins/ai_ui/src/config.tsx; grep -c "settingsNavigation" frontend/plugins/ai_ui/src/config.tsx; grep -c "stub: filled in Phase 2.3" frontend/plugins/ai_ui/src/Copilot.tsx; grep -c "Phase 3.2" frontend/plugins/ai_ui/src/hooks/index.ts; for hook in useAIChat useAIDraft useAISuggest useAIClassify useRagSearch; do grep -q "export const $hook" frontend/plugins/ai_ui/src/hooks/index.ts || (echo "Missing hook export: $hook" && false); done; for widget in AIChatPanel AIDraftEditor AISuggestButton AIRagSearchBox; do grep -q "$widget" frontend/plugins/ai_ui/src/widgets/index.ts || (echo "Missing widget export: $widget" && false); done</automated>
  </verify>
  <done>All 8 files exist; `config.tsx` references `IUIConfig` + `settingsNavigation`; `Copilot.tsx` contains the exact `stub: filled in Phase 2.3` comment string; hooks/index.ts declares all 5 named exports each throwing with the literal `Phase 3.2` message; widgets/index.ts declares all 4 widget named exports returning null.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ship useAIAvailable() hook + <AiRemoteBoundary> in erxes-ui/ai (REQ UI-10) with unit tests for the rejection path</name>
  <files>
    frontend/libs/erxes-ui/src/ai/useAIAvailable.ts,
    frontend/libs/erxes-ui/src/ai/AiRemoteBoundary.tsx,
    frontend/libs/erxes-ui/src/ai/index.ts,
    frontend/libs/erxes-ui/src/ai/__tests__/useAIAvailable.test.tsx,
    frontend/libs/erxes-ui/src/ai/__tests__/AiRemoteBoundary.test.tsx,
    frontend/libs/erxes-ui/src/index.ts
  </files>
  <read_first>
    frontend/libs/erxes-ui/src/index.ts (existing barrel — append the new `./ai` re-export);
    frontend/libs/erxes-ui/src/types/UIConfig.ts (to keep type imports consistent);
    .planning/research/PITFALLS.md §P11 ("ErrorBoundary at every lazy-import call site" guidance);
    .planning/codebase/CONCERNS.md (MF has no retry/fallback — informs why this hook + boundary exist);
    .planning/phases/01.1-shared-module-plugin-scaffolds/01.1-CONTEXT.md §decisions D-12 bullet 14 (`useAIAvailable.ts` in erxes-ui/ai or ui-modules — we pick erxes-ui because it's the existing home for cross-plugin shared types and hooks), §specifics last bullet (caching + refetch policy)
  </read_first>
  <behavior>
    - `useAIAvailable.ts`: Export `function useAIAvailable(): { available: boolean | null; loading: boolean; refetch: () => void }`. Uses a module-scope `Promise<boolean> | null` cache (single in-flight probe per page lifetime). The probe: `import('ai_ui/config')` — on resolve, available = true; on reject (MF load failure, network 404, etc.), available = false. Initial state `available: null, loading: true` (matches sales_ui patterns that initialize loading queries with `null` data). `refetch()` clears the module-scope cache and triggers a re-probe via a React useState `tick` increment that the useEffect dependency picks up. The hook MUST NOT throw — all errors caught and reflected via `available: false`. Cached value persists for the lifetime of the host page per CONTEXT specifics ("cached for the lifetime of the host page").
    - `AiRemoteBoundary.tsx`: Export `function AiRemoteBoundary({ children, fallback }: { children: React.ReactNode; fallback?: React.ReactNode }): JSX.Element`. Wraps `react-error-boundary`'s `<ErrorBoundary>` (already in root package.json as `^5.0.0`). On error, renders `fallback` if provided, otherwise renders a default `<div data-ai-remote-boundary-fallback>AI features are unavailable.</div>` (so consumers can grep for the data-attribute in dev to find unwrapped imports). The boundary catches errors thrown during render of `children` — including the synthetic errors thrown by hooks/widgets stubs (Task 2 — `useAIChat: implementation lands in Phase 3.2`) — so a misuse renders the fallback instead of crashing the host.
    - `frontend/libs/erxes-ui/src/ai/index.ts`: `export { useAIAvailable } from './useAIAvailable'; export { AiRemoteBoundary } from './AiRemoteBoundary';`.
    - `frontend/libs/erxes-ui/src/index.ts`: add `export * from './ai';` to the existing barrel (preserve all existing exports).
    - `useAIAvailable.test.tsx`: Two tests — (1) when `import('ai_ui/config')` resolves successfully (mock via `jest.mock('ai_ui/config', () => ({ default: {} }), { virtual: true })`), `renderHook(() => useAIAvailable())` eventually returns `{ available: true, loading: false, refetch: <fn> }` (use `waitFor` from `@testing-library/react`); (2) when the dynamic import rejects (mock the import with a rejecting promise), hook returns `{ available: false, loading: false, refetch: <fn> }`.
    - `AiRemoteBoundary.test.tsx`: Two tests — (1) wrapping `<div>ok</div>` renders `ok`; (2) wrapping a throwing child component renders the default fallback string `AI features are unavailable.`; (3) wrapping a throwing child with custom `fallback={<span data-x>custom</span>}` renders `custom`.
    Implements REQ UI-10 (consumers see a clean error if AI is disabled; useAIAvailable hook + ErrorBoundary at every ai_ui remote import; no MF hard-fail on missing remote), CONTEXT D-12 bullet 14, CONTEXT specifics bullet 6 (caching + manual refetch yes).
  </behavior>
  <action>
    Create the 5 files in `frontend/libs/erxes-ui/src/ai/`. Append the `export * from './ai';` line to `frontend/libs/erxes-ui/src/index.ts`. Tests use `@testing-library/react`'s `renderHook` and `waitFor`. The `jest.mock('ai_ui/config', ..., { virtual: true })` syntax is required because `ai_ui/config` is not a real module the test environment can resolve (it's MF-served at runtime). Implements UI-10.
  </action>
  <verify>
    <automated>ls frontend/libs/erxes-ui/src/ai/{useAIAvailable.ts,AiRemoteBoundary.tsx,index.ts} frontend/libs/erxes-ui/src/ai/__tests__/{useAIAvailable.test.tsx,AiRemoteBoundary.test.tsx} 2>&1; grep -c "useAIAvailable" frontend/libs/erxes-ui/src/ai/useAIAvailable.ts; grep -c "AiRemoteBoundary" frontend/libs/erxes-ui/src/ai/AiRemoteBoundary.tsx; grep -c "export \* from './ai'" frontend/libs/erxes-ui/src/index.ts; pnpm nx test erxes-ui --testPathPattern='ai/' 2>&1 | tee /tmp/erxes-ui-ai-test.log; grep -q "Tests:.*passed" /tmp/erxes-ui-ai-test.log && grep -qv "Tests:.*failed" /tmp/erxes-ui-ai-test.log || (echo "ai/ tests failed" && cat /tmp/erxes-ui-ai-test.log && false)</automated>
  </verify>
  <done>All 5 files exist; the erxes-ui barrel exports `useAIAvailable` and `AiRemoteBoundary`; `pnpm nx test erxes-ui --testPathPattern='ai/'` runs both test files with all assertions passing (resolve → available: true; reject → available: false; boundary catches throws + renders fallback).</done>
</task>

<task type="auto">
  <name>Task 4: Build ai_ui end-to-end and verify MF expose surface (XCUT-04 frontend half)</name>
  <files>
    (no new files — build verification)
  </files>
  <read_first>
    frontend/plugins/ai_ui/project.json (Nx build target — confirm rspackConfig path);
    frontend/plugins/ai_ui/module-federation.config.ts (verify final state from Task 1+2)
  </read_first>
  <action>
    Run `pnpm nx build ai_ui` and verify exit 0. Inspect `dist/frontend/plugins/ai_ui/` and assert the MF remote entry file (typically `remoteEntry.js` or `mf-manifest.json` per `@module-federation/enhanced` v0.6.6 output) is present, and that the 6 exposed module chunks are emitted (filenames include `config`, `aiSettings`, `Copilot`, `atoms`, `hooks`, `widgets`). Implements the frontend half of REQ XCUT-04 (clean `pnpm nx build ai_ui`). The Phase 1.1 ROADMAP success criterion #3 (HOOK-02: shared block uses singleton + requiredVersion mirroring sales_ui:3-13) is enforced by Task 1's verify gate; this task confirms the build completes with that config.
  </action>
  <verify>
    <automated>pnpm nx build ai_ui 2>&1 | tail -30; ls dist/frontend/plugins/ai_ui/ 2>&1 | head -30; ls dist/frontend/plugins/ai_ui/ 2>&1 | grep -cE "remoteEntry|mf-manifest" | grep -q '^[1-9]' || (echo "No MF remote entry artifact found" && false); for module in config aiSettings Copilot atoms hooks widgets; do ls dist/frontend/plugins/ai_ui/ | grep -qi "$module" || echo "(informational) module chunk not literally named $module — check actual chunk names"; done</automated>
  </verify>
  <done>`pnpm nx build ai_ui` exits 0; `dist/frontend/plugins/ai_ui/` contains the MF remote entry artifact (`remoteEntry.js` or `mf-manifest.json`); the build emits chunks corresponding to all 6 exposes (chunk filenames are bundler-controlled — informational check only).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host plugin UI → ai_ui MF remote | When `ai` is disabled or `ai_ui` fails to load (network/build issue), consumers calling `import('ai_ui/...')` must NOT crash the host. The `useAIAvailable` hook + `AiRemoteBoundary` provide the graceful-degradation contract. |
| MF singleton config → React/Apollo/Jotai state | Cross-plugin shared libraries must use a single instance to keep state coherent (e.g., one Jotai store across `sales_ui` + `ai_ui` + host). The verbatim mirror of `sales_ui:3-13` ensures this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-09 | Denial of Service | `ai_ui` MF hard-fail crashing host | mitigate | `useAIAvailable` + `<AiRemoteBoundary>` shipped in `erxes-ui/ai` (Task 3). Other plugin UIs gate AI imports behind the hook and wrap remote-imported children with the boundary. CI smoke for the cross-plugin singleton path lands in Phase 1.4 / 2.3 (per PITFALLS §P11). |
| T-01.1-10 | Tampering | MF version drift (React 19 future) | mitigate | `singleton: true` + `requiredVersion` (from defaultConfig injection); NO `strictVersion: true` per D-08 — tolerates minor drift, surfaces major mismatches as warnings. Cross-plugin smoke test deferred to Phase 1.4 / 2.3 per CONTEXT scope-reduction prohibition. |
| T-01.1-11 | Information Disclosure | Stub hook error messages | accept | The stubbed hooks throw `useAIChat: implementation lands in Phase 3.2` — a developer-visible message, not user-supplied data; safe to surface. |
| T-01.1-SC | Tampering | npm/pnpm installs | mitigate | This plan adds NO new frontend deps (per STACK.md "Zero new frontend dependencies are required for ai_ui"). All hooks/widgets/boundary use libraries already in the monorepo (react@18.3.1, react-error-boundary@^5.0.0, @testing-library/react@15.0.6, jotai@^2.12.0, @apollo/client@^3.11.8). |
</threat_model>

<verification>
- `pnpm nx build ai_ui` exits 0 (XCUT-04 frontend half)
- `frontend/plugins/ai_ui/module-federation.config.ts` exposes exactly 6 entries (config/aiSettings/Copilot/atoms/hooks/widgets per D-06)
- `module-federation.config.ts` shared block: all 9 coreLibraries entries from sales_ui:3-13 present verbatim; NO `strictVersion: true` (HOOK-02)
- Phase 1.1 placeholders per D-07: `Copilot.tsx` returns `<></>` with exact `stub: filled in Phase 2.3` comment; `atoms/index.ts` is empty typed object; `hooks/index.ts` throws `Phase 3.2` per accessor; `widgets/index.ts` widgets return null
- `pnpm nx test erxes-ui --testPathPattern='ai/'` passes both `useAIAvailable.test.tsx` (resolve+reject paths) and `AiRemoteBoundary.test.tsx` (no-error+throw+custom-fallback paths)
- `frontend/libs/erxes-ui/src/index.ts` re-exports `useAIAvailable` + `AiRemoteBoundary` via `export * from './ai'` (consumers `import { useAIAvailable, AiRemoteBoundary } from 'erxes-ui'`)
</verification>

<success_criteria>
- Phase 1.1 ROADMAP success criterion #1 (clean `pnpm nx build ai_ui`) — passes via Task 4
- Phase 1.1 ROADMAP success criterion #3 (HOOK-02 MF singleton + requiredVersion mirroring sales_ui:3-13) — passes via Task 1 verify gate
- Phase 1.1 ROADMAP success criterion #4 (`useAIAvailable()` returns false when `ai_ui` remote fails; ErrorBoundary pattern shipped) — passes via Task 3
- REQ HOOK-02 (shared singleton + requiredVersion config) — passes
- REQ UI-10 (useAIAvailable + ErrorBoundary at every ai_ui remote import; no MF hard-fail) — passes
- REQ XCUT-04 (frontend half of clean build chain) — passes
- D-06 (final 6-entry MF expose surface day 1) — passes
- D-07 (placeholder bodies for not-yet-built exposes) — passes
- D-08 (singleton + requiredVersion, NO strictVersion) — passes
- D-12 (frontend file inventory: module-federation.config.ts, rspack.config.ts, project.json, tsconfig.json, src/{main.ts, bootstrap.tsx, config.tsx, modules/Main.tsx, Copilot.tsx, widgets/index.ts, hooks/index.ts, atoms/index.ts}, useAIAvailable.ts) — passes
- D-14 (manual copy of sales_ui, not `pnpm create-plugin`) — passes
- PITFALLS §P11 (singleton: true + requiredVersion mandatory; NO strictVersion: true) — passes; cross-plugin smoke test for single React instance deferred to Phase 1.4 / 2.3 per CONTEXT
</success_criteria>

<output>
On completion, create `.planning/phases/01.1-shared-module-plugin-scaffolds/01.1-03-SUMMARY.md` per the standard summary template. The SUMMARY MUST note:
1. The 6-entry MF expose surface is FINAL — Phase 1.4 fills the `./aiSettings` page body; Phase 2.3 fills the `./Copilot` body; Phase 3.2 fills the `./hooks` and `./widgets` bodies. The MF config in this plan should NOT change in those later phases.
2. The `useAIAvailable` hook lives at `frontend/libs/erxes-ui/src/ai/useAIAvailable.ts` (not `ui-modules` — pick documented in plan per CONTEXT D-12 bullet 14 Claude's-Discretion). The decision rationale: `erxes-ui` is the existing home for cross-plugin shared hooks (per the existing `frontend/libs/erxes-ui/src/hooks/` directory pattern) and the AI-aware boundary belongs alongside the type definitions for `IUIConfig` which is also here.
3. The Phase 1.1 hooks/widgets stubs throw / return-null intentionally — they are typed scaffolding, NOT functional. Plugin developers who try to call `useAIChat` from sales_ui before Phase 3.2 will see a clear `Phase 3.2` error message captured by `AiRemoteBoundary`.
</output>