---
phase: 01.1-shared-module-plugin-scaffolds
status: passed
score: 6/6 success criteria verified
verified_at: 2026-05-21T06:04:29Z
overrides_applied: 0
---

# Phase 1.1: Shared Module + Plugin Scaffolds — Verification Report

**Phase Goal:** Stand up the cross-cutting foundation every later phase depends on — `erxes-api-shared/src/ai/` shared module, `ai_api`/`ai_ui` plugin skeletons, Docker base, env vars, and the typed graceful-disable shim so other plugins can compile against `trpc.ai.*` even when `ai` is excluded from `ENABLED_PLUGINS`. No business logic, no provider calls, no UI behavior — only the scaffolding that lets Phases 1.2+ proceed.

**Verified:** 2026-05-21T06:04:29Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `pnpm nx build erxes-api-shared && pnpm nx build ai_api && pnpm nx build ai_ui` succeeds | VERIFIED | Build artifacts confirmed on disk: `backend/erxes-api-shared/ai/dist/erxes-api-shared-ai.{cjs.js,esm.js,cjs.d.ts}`, `backend/plugins/ai_api/dist/src/main.js`, `dist/frontend/plugins/ai_ui/remoteEntry.js` + `mf-manifest.json` + 6 expose chunks |
| 2 | With `ENABLED_PLUGINS=sales`, `createAIClient(subdomain).llm.chat(...)` throws `AINotEnabledError` with actionable message | VERIFIED | `shim.ts` proxy checks `isEnabled('ai')` (60s TTL cache) at every leaf method; throws `AINotEnabledError` with exact message `"AI plugin not enabled. Add 'ai' to ENABLED_PLUGINS and restart the gateway. Docs: https://erxes.io/docs/ai-plugin-setup"`. Proven by `shim.test.ts` test 2 (exact message match, instanceof checks). Tests are hermetic — no Redis. |
| 3 | `ai_ui` MF config declares 9 coreLibraries as singletons with `requiredVersion`, no `strictVersion: true` | VERIFIED | `frontend/plugins/ai_ui/module-federation.config.ts` is a verbatim mirror of `sales_ui:3-13`: 9-entry `coreLibraries` Set (`react`, `react-dom`, `react-router`, `react-router-dom`, `erxes-ui`, `@apollo/client`, `jotai`, `ui-modules`, `react-i18next`). `shared` callback returns `defaultConfig` (which auto-injects `singleton: true` + `requiredVersion`). `grep -c "strictVersion: true"` = 0. |
| 4 | `useAIAvailable()` returns `false` on remote load failure; every consumer import of `ai_ui` remotes is wrapped in `AiRemoteBoundary` | VERIFIED | `frontend/libs/erxes-ui/src/ai/useAIAvailable.ts` — module-scope probe cache, never throws, returns `{available: boolean|null, loading, refetch}`. `AiRemoteBoundary.tsx` wraps `react-error-boundary`. Both exported from `erxes-ui` barrel. Tests: 3 probe cases + 3 boundary cases all pass. |
| 5 | Every subdomain-keyed surface in Phase 1.1 includes `subdomain` by construction | VERIFIED | Phase 1.1 declares NO collections, Redis keys, queues, or PubSub channels — this criterion is vacuously satisfied for this phase. The single subdomain-sensitive API surface shipped is `encryptSecret(plaintext, subdomain)` and `decryptSecret(ciphertext, subdomain)` where `subdomain` is a required positional TypeScript argument — callers cannot omit it without a compile error. |
| 6 | `docker-compose.yml` gains optional `qdrant` under `profiles: ['ai']`; `.env.sample` lists all 11 AI env vars | VERIFIED | `docker-compose.yml` exists at repo root, `qdrant/qdrant:v1.13.0` under `profiles: ['ai']`. `.env.sample` contains all 11 vars (`AI_API_PORT=3320`, `AI_UI_PORT=3020`, `ERXES_SECRET=`, `AI_DEFAULT_VECTOR_BACKEND=qdrant`, `QDRANT_URL=http://localhost:6333`, `AI_EMBED_CONCURRENCY=4`, `AI_DEFAULT_CHAT_MODEL=`, `AI_DEFAULT_EMBED_MODEL=`, `AI_BUDGET_DEFAULT_USD=50`, `AI_MAX_TOOL_STEPS=8`, `AI_GUARDRAILS_STRICT=false`) plus `ENABLED_PLUGINS=operation,ai,sales,frontline` example comment. |

**Score: 6/6 truths verified**

---

## Required Artifacts

| Artifact | Expected | Status | Evidence |
|----------|----------|--------|----------|
| `backend/erxes-api-shared/src/ai/index.ts` | Public re-exports for `erxes-api-shared/ai` subpath | VERIFIED | File exists; exports `createAIClient`, `AINotEnabledError`, `encryptSecret`, `decryptSecret`, `redactKey`, `redactPii`, `rehydratePii`, `COST_TABLE`, `DRIFT_MULTIPLIER`, `ILLMProvider`, `PROVIDER_REGISTRY`, plus all types from `./types` |
| `backend/erxes-api-shared/src/ai/shim.ts` | `createAIClient` Proxy + `AINotEnabledError` | VERIFIED | Implements D-03/D-04: sync factory, JS Proxy, `isEnabled('ai')` check with 60s TTL cache keyed by subdomain. `AINotEnabledError extends Error` with `name='AINotEnabledError'` |
| `backend/erxes-api-shared/src/ai/encryption.ts` | AES-256-GCM + `scryptSync(ERXES_SECRET, subdomain, 32)` + versioned envelope | VERIFIED | `encryptSecret`/`decryptSecret` with `scryptSync`. Envelope `{version: 1, iv, ciphertext, authTag}` base64-encoded JSON. 12-byte IV, 16-byte auth tag. Cross-subdomain decryption fails via auth-tag mismatch. |
| `backend/erxes-api-shared/src/ai/redaction.ts` | 5-pattern `redactKey` + `redactPii`/`rehydratePii` wrappers | VERIFIED | Combined-alternation regex: Anthropic `sk-ant-` before generic `sk-`, plus `gsk_`, `AIza`, 32-hex Azure. `REDACTED_PLACEHOLDER = '***REDACTED***'`. PII wrappers are no-op in Phase 1.1 with correct public type surface. |
| `backend/erxes-api-shared/src/ai/tokens.ts` | `COST_TABLE` + `DRIFT_MULTIPLIER` + lazy tiktoken | VERIFIED | 5 seeded models (`openai:gpt-4o`, `openai:gpt-4o-mini`, `openai:text-embedding-3-small`, `anthropic:claude-3-5-sonnet-latest`, `google:gemini-1.5-pro`). `DRIFT_MULTIPLIER`: openai/azure=1.0, anthropic=1.15, google/ollama=1.1, custom=1.2. `getTiktokenModule()` gated by `NODE_ENV !== 'test'`. |
| `backend/erxes-api-shared/src/ai/providers/base.ts` | `ILLMProvider` interface only, no SDK imports | VERIFIED | Interface with `chat()`, `embed()`, `listModels()`. No provider SDK imports. |
| `backend/erxes-api-shared/src/ai/types.ts` | `ChatMessage`, `ToolDef`, `AiToolDef`, `Usage`, `ProviderError` + Zod schemas | VERIFIED | All types present. Every Zod object schema uses `.strict()` (9 confirmed occurrences). `ProviderError extends Error` with `providerKind`, `model`, `statusCode`. |
| `backend/erxes-api-shared/package.json` | Preconstruct entrypoint `./ai/index.ts` + 4 pinned deps | VERIFIED | `preconstruct.entrypoints` array contains `"./ai/index.ts"`. Deps: `tiktoken@1.0.22`, `@anthropic-ai/tokenizer@0.0.4`, `redact-pii-light@1.0.0`, `zod-to-json-schema@3.23.5`. Zero provider SDKs. |
| `backend/erxes-api-shared/ai/package.json` | Preconstruct subpath package manifest | VERIFIED | `{main, module, types}` pointing to `dist/erxes-api-shared-ai.{cjs.js,esm.js,cjs.d.ts}`. Build artifacts confirmed in `ai/dist/`. |
| `backend/plugins/ai_api/src/main.ts` | `startPlugin({ name: 'ai', port: 3320 })` + `validateErxesSecret` BEFORE startPlugin | VERIFIED | `validateErxesSecret` named export runs at module-load before `startPlugin(...)`. Exact error message: `"ERXES_SECRET must be >= 32 hex bytes (got N). Generate with \`openssl rand -hex 32\`."` Process exits code 1. `meta: {}` empty. |
| `backend/plugins/ai_api/Dockerfile` | `FROM node:22-alpine3.22` (both stages) | VERIFIED | Both `FROM` lines use `node:22-alpine3.22` per D-09 ADDENDUM (monorepo standard supersedes original `node:18.20-alpine`). Multi-stage build, `USER 1000` in final stage. |
| `backend/plugins/ai_api/src/{modules,meta,workers}/.gitkeep` | Reserved empty directories | VERIFIED | All three `.gitkeep` files exist at 0 bytes. |
| `backend/plugins/ai_api/src/__tests__/erxes-secret-validation.test.ts` | 7 test cases for `validateErxesSecret` | VERIFIED | 7 cases: undefined, empty, 31-byte, 32-byte, 64-byte, versioned multi-secret (ok), versioned multi-secret first entry too short (fail). |
| `docker-compose.yml` | `qdrant` under `profiles: ['ai']` | VERIFIED | File is NEW (did not exist before Phase 1.1). `qdrant/qdrant:v1.13.0`, ports 6333+6334, `qdrant_storage` volume. |
| `.env.sample` | 11 AI env vars + `ENABLED_PLUGINS` ai example | VERIFIED | All 11 vars present. `ENABLED_PLUGINS=operation,ai,sales,frontline` example present. |
| `frontend/plugins/ai_ui/module-federation.config.ts` | 6-entry expose map + verbatim `coreLibraries` from sales_ui:3-13 | VERIFIED | 6 exposes: `./config`, `./aiSettings`, `./Copilot`, `./atoms`, `./hooks`, `./widgets`. `coreLibraries` Set verbatim mirror of sales_ui lines 3-13. No `strictVersion: true`. |
| `frontend/plugins/ai_ui/src/config.tsx` | `CONFIG: IUIConfig` with `settingsNavigation` | VERIFIED | `IUIConfig`, `name: 'ai'`, `path: 'ai'`, `icon: IconRobot`, lazy `Main` + `AISettingsNavigation`, `settingsNavigation` callback, `navigationGroup`. |
| `frontend/plugins/ai_ui/src/Copilot.tsx` | Returns `<></>` with `// stub: filled in Phase 2.3` | VERIFIED | Exact literal `stub: filled in Phase 2.3` in comment. `export default function Copilot() { return <></>; }` |
| `frontend/plugins/ai_ui/src/hooks/index.ts` | 5 named hook exports throwing `Phase 3.2` errors | VERIFIED | `useAIChat`, `useAIDraft`, `useAISuggest`, `useAIClassify`, `useRagSearch` each throw `useXxx: implementation lands in Phase 3.2`. Default export object provided. |
| `frontend/plugins/ai_ui/src/widgets/index.ts` | 4 widget exports returning `null` | VERIFIED | `AIChatPanel`, `AIDraftEditor`, `AISuggestButton`, `AIRagSearchBox` — each `FC` returning `null`. Default export object provided. |
| `frontend/plugins/ai_ui/src/atoms/index.ts` | Empty typed atom map | VERIFIED | `export const aiAtoms = {} as const; export default aiAtoms;` |
| `frontend/libs/erxes-ui/src/ai/useAIAvailable.ts` | MF-load probe hook `{available, loading, refetch}` | VERIFIED | Module-scope probe cache. `aiAvailableProbe` injection seam for tests. `resetAIAvailableCache()` exported. Never throws — all errors caught into `available: false`. |
| `frontend/libs/erxes-ui/src/ai/AiRemoteBoundary.tsx` | ErrorBoundary for ai_ui remote imports | VERIFIED | Wraps `react-error-boundary`'s `<ErrorBoundary>`. Default fallback `<div data-ai-remote-boundary-fallback>AI features are unavailable.</div>`. Custom `fallback` prop override. |
| `frontend/libs/erxes-ui/src/index.ts` | `export * from './ai'` added | VERIFIED | Line 9: `export * from './ai';`. Consumers can `import { useAIAvailable, AiRemoteBoundary } from 'erxes-ui'`. |

---

## Key Link Verification

| From | To | Via | Status | Evidence |
|------|----|-----|--------|----------|
| `shim.ts` | `utils/service-discovery.ts` | `import { isEnabled } from '../utils/service-discovery'` | WIRED | Line 16 of shim.ts: `import { isEnabled } from '../utils/service-discovery';` |
| `index.ts` | `shim.ts` | Named re-export of `createAIClient` + `AINotEnabledError` | WIRED | `export { createAIClient, AINotEnabledError } from './shim';` present in index.ts |
| `package.json` | `src/ai/index.ts` | preconstruct.entrypoints array | WIRED | `"./ai/index.ts"` confirmed in `preconstruct.entrypoints`; `ai/package.json` + build artifacts present |
| `ai_api/main.ts` | `erxes-api-shared/utils` | `import { startPlugin } from 'erxes-api-shared/utils'` | WIRED | Line 1 of main.ts confirmed |
| `ai_api/main.ts` | `process.env.ERXES_SECRET` | `validateErxesSecret` called before `startPlugin` | WIRED | Lines 55–60: `const __secretCheck = validateErxesSecret(process.env.ERXES_SECRET); if (!__secretCheck.ok) { ... process.exit(1); }` before `startPlugin({...})` |
| `ai_ui/module-federation.config.ts` | `sales_ui/module-federation.config.ts` | `coreLibraries` verbatim mirror | WIRED | Both files contain identical 9-entry Set in same order |
| `ai_ui/src/config.tsx` | `erxes-ui IUIConfig` | `import { IUIConfig } from 'erxes-ui'` | WIRED | Line 4 of config.tsx confirmed |
| `erxes-ui/src/ai/useAIAvailable.ts` | `ai_ui/config` MF expose | `import('ai_ui/config' as string)` dynamic import probe | WIRED | `defaultProbe` function at line 32 of useAIAvailable.ts |

---

## Data-Flow Trace (Level 4)

Not applicable. Phase 1.1 ships scaffolding only — no dynamic data rendering, no API calls that return real data, no UI that presents live state to users. All scaffold components return stubs by design (correct Phase 1.1 behavior).

---

## Behavioral Spot-Checks

| Behavior | Evidence | Status |
|----------|----------|--------|
| `erxes-api-shared/ai` build artifacts (CJS + ESM) on disk | `backend/erxes-api-shared/ai/dist/erxes-api-shared-ai.cjs.js`, `.esm.js`, `.cjs.d.ts` confirmed present | PASS |
| `ai_api` dist artifact on disk | `backend/plugins/ai_api/dist/src/main.js` confirmed present | PASS |
| `ai_ui` MF remote entry + 6 expose chunks on disk | `dist/frontend/plugins/ai_ui/remoteEntry.js`, `mf-manifest.json`, `__federation_expose_{config,aiSettings,Copilot,atoms,hooks,widgets}.js` all confirmed | PASS |
| No provider SDK imports in Phase 1.1 ai source files | `grep` for `openai`, `@anthropic-ai/sdk`, `@google/generative-ai` in `src/ai/` — zero matches | PASS |
| `validateErxesSecret` test suite (7 cases) | `backend/plugins/ai_api/src/__tests__/erxes-secret-validation.test.ts` ships with all 7 cases as documented | PASS |

Step 7b: SKIPPED — no live runnable entry points testable without Mongo/Redis. Build-artifact checks above substitute.

---

## Probe Execution

Step 7c: No probe scripts declared in any PLAN or SUMMARY for this phase. Probe execution step: SKIPPED (no `scripts/*/tests/probe-*.sh` files exist).

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| XCUT-01 | 01.1-01, 01.1-02 | Subdomain everywhere — cross-tenant access impossible by construction | SATISFIED | Phase 1.1 ships no collections/queues/channels. `encryptSecret(plaintext, subdomain)` requires subdomain as typed positional arg — TS prevents omission |
| XCUT-02 | 01.1-01 | Graceful disable shim — `AINotEnabledError` with actionable message | SATISFIED | `createAIClient(subdomain)` proxy + `AINotEnabledError` with exact message + `isEnabled('ai')` check. Proven by shim.test.ts |
| XCUT-04 | 01.1-01, 01.1-02, 01.1-03 | Clean build chain: `pnpm nx build erxes-api-shared && ai_api && ai_ui` | SATISFIED | Build artifacts on disk. SUMMARY documents all three builds exit 0 |
| XCUT-10 | 01.1-02 | Dockerfile + docker-compose + .env.sample complete | SATISFIED | Dockerfile uses `node:22-alpine3.22` (aligned to monorepo standard per D-09 ADDENDUM — REQUIREMENTS.md text says `node:18.20-alpine` but D-09 ADDENDUM pre-resolved this to `node:22-alpine3.22` before execution; see Deviations section). docker-compose.yml + .env.sample confirmed |
| UI-10 | 01.1-03 | `useAIAvailable()` + ErrorBoundary at every ai_ui remote import; no MF hard-fail | SATISFIED | `useAIAvailable.ts` + `AiRemoteBoundary.tsx` shipped in `erxes-ui/ai`; re-exported from erxes-ui barrel; 6 tests pass |
| HOOK-02 | 01.1-03 | `shared` config: `singleton: true` + `requiredVersion` matching root `package.json`; no `strictVersion: true` | SATISFIED | `coreLibraries` verbatim mirror of sales_ui:3-13. `shared` callback returns `defaultConfig` (auto-injects singleton + requiredVersion). `strictVersion: true` count = 0 |

---

## Locked Decision Verification (D-01 through D-15)

| Decision | Verified | Evidence |
|----------|----------|----------|
| D-01: AI module at `erxes-api-shared/src/ai/` with `"./ai"` preconstruct subpath export | PASS | `package.json` has `"./ai/index.ts"` in `preconstruct.entrypoints`; `ai/package.json` + build artifacts present |
| D-02: Import via `from 'erxes-api-shared/ai'` exclusively (not `utils/ai`) | PASS | All imports in tests and source use `'erxes-api-shared/ai'` path. SUMMARY confirms smoke import from sales_api tsconfig passed zero TS errors |
| D-03: `createAIClient` is synchronous factory returning typed JS Proxy | PASS | `shim.ts` returns plain object `{llm, rag, agent, tools, prompt, audit}` where each namespace is a `new Proxy(...)`. No await on factory call. `shim.test.ts` test 1 asserts typeof === 'object' without await |
| D-04: `isEnabled('ai')` from service-discovery with 60s TTL cache per subdomain | PASS | `shim.ts` lines 43–60: `checkAiEnabled()` with `enabledCache` Map and 60s TTL. Imports `isEnabled` from `'../utils/service-discovery'` |
| D-05: Full TypeScript surface declared (`llm/rag/agent/tools/prompt/audit`) | PASS | `NAMESPACE_METHODS` record in shim.ts lists all 6 namespaces with their method names |
| D-06: Phase 1.1 ships final 6-entry MF expose surface | PASS | `module-federation.config.ts` exposes: `./config`, `./aiSettings`, `./Copilot`, `./atoms`, `./hooks`, `./widgets`. Header comment: "Phase 1.1 MF surface is FINAL" |
| D-07: Phase 1.1 placeholders — Copilot `<></>`, atoms `{}`, hooks throw, widgets `null` | PASS | `Copilot.tsx`: `return <></>` with `// stub: filled in Phase 2.3`. `atoms/index.ts`: `{} as const`. `hooks/index.ts`: 5 exports each throwing `Phase 3.2`. `widgets/index.ts`: 4 FCs returning `null` |
| D-08: `shared` block verbatim mirror of `sales_ui:3-13`, no `strictVersion: true` | PASS | Confirmed verbatim (same 9 entries, same order, same callback pattern). Zero `strictVersion: true` occurrences |
| D-09 ADDENDUM: Dockerfile uses `node:22-alpine3.22` (not `node:18.20-alpine`) | PASS | Both `FROM` lines in Dockerfile confirmed as `node:22-alpine3.22` |
| D-10: `ERXES_SECRET` validation NOT in `erxes-api-shared` startup | PASS | Validation is in `ai_api/src/main.ts` only. `erxes-api-shared/src/ai/` has no `ERXES_SECRET` length check |
| D-11: Shim's `createAIClient()` does NOT re-validate `ERXES_SECRET` | PASS | `shim.ts` has no reference to `ERXES_SECRET` |
| D-12: Full file inventory shipped | PASS | All listed files verified present on disk (see Required Artifacts table above) |
| D-13: Only 4 pinned deps added; zero provider SDKs | PASS | `package.json` deps: `tiktoken@1.0.22`, `@anthropic-ai/tokenizer@0.0.4`, `redact-pii-light@1.0.0`, `zod-to-json-schema@3.23.5`. `grep` for `openai`, `@anthropic-ai/sdk`, `@google/generative-ai` in package.json = 0 matches |
| D-14: Manual copy of `sales_api`/`sales_ui` (not `pnpm create-plugin`) | PASS | SUMMARY documents manual copy approach; no `pnpm create-plugin` invocation |
| D-15: Test coverage on `src/ai/` >= 80% | PASS | SUMMARY reports 88.35% line coverage across 4 test suites, 34 tests. Additional `surface.test.ts` suite added to meet the gate (documented deviation from the planned 3-suite minimum) |

---

## Pitfall Acceptance Gates

| Gate | Requirement | Status | Evidence |
|------|-------------|--------|----------|
| P1: `redactKey` covers exactly 5 provider patterns | Anthropic `sk-ant-` matched BEFORE generic `sk-`; Groq `gsk_`; Google `AIza`; Azure 32-hex | PASS | `redaction.ts` uses combined alternation regex with Anthropic first. `redaction.test.ts` has 5 dedicated `it()` blocks, one per pattern, each asserting redaction to `***REDACTED***` and absence of original key. Anthropic test specifically asserts `ant-` is not left visible. |
| P11: MF singleton + requiredVersion, NO `strictVersion: true` | All 9 coreLibraries declared with singleton + requiredVersion; zero strict-version flags | PASS | `coreLibraries` Set in place. `defaultConfig` from `@nx/rspack/module-federation` auto-injects `singleton: true` + `requiredVersion`. `grep -c "strictVersion: true"` = 0. Comment in file even spells out "NO strict-version flag per PITFALLS §P11" |
| P13: First-save key leak prevention | `encryptSecret` must not be callable without `ERXES_SECRET` | PASS | `encryption.ts`: `deriveKey()` throws `'ERXES_SECRET environment variable is not set.'` if `!secret`. Test case covers missing `ERXES_SECRET`. Phase 1.1 adds no Provider model (provider API keys land in Phase 1.2), so the first-save leak surface does not exist yet. |
| P14: Versioned AES-256-GCM envelope `{version:1, iv, ciphertext, authTag}` | Rotation-safe envelope ships in 1.1 even though rotation script lands in Phase 1.2 | PASS | `ENCRYPTION_ENVELOPE_VERSION = 1`. Envelope is base64-encoded JSON with exactly `{version, iv, ciphertext, authTag}`. `encryption.test.ts` asserts: sorted keys = `['authTag', 'ciphertext', 'iv', 'version']`, `version === 1`, IV = 12 bytes, authTag = 16 bytes. Unsupported version test also passes. |

---

## Anti-Patterns Found

No blocking anti-patterns detected.

| Pattern | Check | Result |
|---------|-------|--------|
| Unreferenced `TBD`/`FIXME`/`XXX` in phase-modified files | grep across all 8 `src/ai/` files + main.ts + Dockerfile + MF config | 0 occurrences |
| Unreferenced `TODO`/`HACK`/`PLACEHOLDER` | grep across ai module source | 0 relevant occurrences (only `REDACTED_PLACEHOLDER` constant name, not a debt marker) |
| Provider SDK imports in Phase 1.1 `src/ai/` files | grep for `openai`, `@anthropic-ai/sdk`, `@google/generative-ai` | 0 occurrences (only string literals like `'openai:gpt-4o'` as COST_TABLE keys — not SDK imports) |
| `strictVersion: true` in MF config | grep in `module-federation.config.ts` | 0 occurrences |

---

## Human Verification Required

None. All Phase 1.1 success criteria are verifiable programmatically via code inspection and build artifact checks. The phase ships scaffolding only — no visual appearance, no user flows, no real-time behavior, no external service integration in Phase 1.1 scope.

---

## Deviations Noted in SUMMARYs (Forward to Phase 1.2 Planning)

### Plan 01 (erxes-api-shared/ai)

1. **4th test suite `surface.test.ts` added** — The plan specified 3 test files but coverage on `src/ai/` was 53.79% with only `shim`, `encryption`, `redaction` suites (leaving `tokens.ts`, `types.ts`, `providers/index.ts`, `index.ts` at 0%). A 4th suite `surface.test.ts` (12 tests) was added to reach 88.35% coverage, satisfying the >=80% gate. Not a deviation from goal — additive.

2. **preconstruct prompted for `ai/package.json` interactively** — Build hung at preconstruct's prompt. Resolved by pre-creating `backend/erxes-api-shared/ai/package.json` manually and updating `project.json` to declare it in Nx outputs. The file is now checked in alongside `utils/package.json`, `core-types/package.json`, `core-modules/package.json` (same convention).

3. **`surface.test.ts` `getTiktokenModule` test fixed for Nx-set NODE_ENV** — When run via `pnpm nx test`, Nx sets `NODE_ENV=development` not `test`. Test now explicitly sets `NODE_ENV='test'` for the assertion with a try/finally restore.

### Plan 02 (ai_api plugin)

4. **`docker-compose.yml` is a NEW FILE** — CONTEXT D-12 wording assumed it pre-existed. File did not exist; created new. Impact: any future plan adding services should EXTEND this file. (Logged in SUMMARY)

5. **Dockerfile uses `node:22-alpine3.22` not `node:18.20-alpine`** — D-09 ADDENDUM resolved this before execution by aligning to the monorepo standard. REQUIREMENTS.md text for XCUT-10 still reads `node:18.20-alpine` (documentation lag only — the intent of XCUT-10 is satisfied; the REQUIREMENTS.md text should be updated in Phase 3.2 docs pass as part of XCUT-09).

6. **7th test case added to `validateErxesSecret`** — PLAN specified 6 cases; 7th case added (versioned multi-secret where first entry is too short → fail). Tightens coverage of the negative path in the multi-secret format. Additive.

7. **`tsconfig.build.json` gained `**/*.test.ts` + `src/__tests__/**` exclusions** — Without these, production build would emit test files into `dist/`. Caught proactively.

8. **`jest.config.ts` gained `moduleNameMapper`** — Required for ts-jest to resolve `~/*`, `@/*`, `erxes-api-shared/*` path aliases. Not in sales_api template (which has no jest config at all).

### Plan 03 (ai_ui + erxes-ui/ai)

9. **Test seam injection pattern (`aiAvailableProbe.impl`)** — The planned `jest.mock('ai_ui/config', ..., { virtual: true })` + `jest.resetModules()` pattern caused `TypeError: Cannot read properties of null (reading 'useState')` in the refetch test. Root cause: `jest.resetModules()` creates a new React instance that fights `@testing-library/react`'s singleton dispatcher. Resolution: `aiAvailableProbe = { impl: () => ... }` mutable holder exported from `useAIAvailable.ts`. Tests swap the impl directly without module resets. Public contract of the hook is unchanged.

10. **`aiAvailableProbe` exported from `useAIAvailable.ts` directly but NOT re-exported from `ai/index.ts`** — Tests import from `'../useAIAvailable'` directly (not through the barrel). The barrel (`ai/index.ts`) exports only the consumer-facing surface (`useAIAvailable`, `resetAIAvailableCache`, `AIAvailability`, `AiRemoteBoundary`). The `aiAvailableProbe` test seam is intentionally not on the public API surface.

11. **`pnpm nx test erxes-ui` fails** — Pre-existing repo quirk: `frontend/libs/erxes-ui/package.json` has `scripts.test = "echo 'Error: no test specified' && exit 1"` which overrides the Nx auto-target. Tests were verified via `pnpm exec jest --config frontend/libs/erxes-ui/jest.config.ts --testPathPattern='ai/__tests__'` instead. Logged to `deferred-items.md`.

12. **Pre-existing TS errors in `frontend/libs/erxes-ui/`** — 5 files with pre-existing TS errors unrelated to AI Kernel (documented in `deferred-items.md`). Out of scope per executor rules.

---

## Gaps Summary

No gaps. All 6 ROADMAP success criteria are verified by direct code inspection of the actual files shipped. All 15 CONTEXT decisions are confirmed implemented. All 6 Phase 1.1 requirements (XCUT-01, XCUT-02, XCUT-04, XCUT-10, UI-10, HOOK-02) are satisfied. All 4 pitfall acceptance gates (P1, P11, P13, P14) pass. No blocking anti-patterns found.

Phase 1.1's goal — "scaffolding that compiles" — is achieved. No business logic was expected or delivered.

---

_Verified: 2026-05-21T06:04:29Z_
_Verifier: Claude (gsd-verifier)_
