// Phase 1.1 stub — Phase 1.4 will render Providers/Models/Budgets/Audit tabs here.
// Exposed as the `./aiSettings` MF entry (default export consumed by the host).
export default function AiSettingsIndexPage() {
  return (
    <div data-ai-settings-index-stub>
      AI Settings (Phase 1.4 will render Providers/Models/Budgets/Audit tabs here)
    </div>
  );
}
