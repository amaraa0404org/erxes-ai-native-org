// Phase 1.1 stubs — every widget returns null. Phase 3.2 replaces the bodies
// with real implementations. Exposed as the `./widgets` MF entry.
import type { FC } from 'react';

export const AIChatPanel: FC = () => null;
export const AIDraftEditor: FC = () => null;
export const AISuggestButton: FC = () => null;
export const AIRagSearchBox: FC = () => null;

export default {
  AIChatPanel,
  AIDraftEditor,
  AISuggestButton,
  AIRagSearchBox,
};
